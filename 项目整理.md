# 🧹 项目整理方案

## 一、当前问题

你的项目有 **3个配置文件、5+个报告脚本、2个回测版本、2个信号脚本**，互相冲突导致数据混乱。

---

## 二、需要做的事（按顺序执行）

### 第1步：统一配置文件

你只需要 `config_v31.yaml`，把它复制为主配置：

```batch
copy config_v31.yaml config.yaml /Y
```

可以删除或重命名：
- `config_optimized.yaml` → 删除或改名 `config_optimized.yaml.bak`

### 第2步：清理旧数据

你的特征数据包含2017年的旧数据，需要重新生成：

```batch
del data\features\features_daily.parquet
del data\backtests\backtest_strategy_v3_equity.csv
del data\backtests\backtest_strategy_v3_trades.csv
del data\backtests\backtest_strategy_v2_equity.csv
del data\backtests\backtest_strategy_v2_trades.csv
```

### 第3步：重新跑完整流程

```batch
python run_all_daily.py
python run_backtest_strategy_v3.py
python run_generate_report.py
```

### 第4步：归档旧脚本

创建一个 `_archive` 文件夹，把不再需要的脚本移进去：

```batch
mkdir _archive

move run_backtest_strategy_v2.py _archive\
move run_backtest_report.py _archive\
move run_generate_chart_v2.py _archive\
move run_generate_chart_v3.py _archive\
move run_generate_daily_report.py _archive\
move run_ai_report.py _archive\
move run_ai_report_v2.py _archive\
move run_deepseek_report.py _archive\
move run_make_daily_rank_v31.py _archive\
move config_optimized.yaml _archive\
```

---

## 三、整理后你只需要关心的文件

### 每日运行（2个命令搞定）
| 命令 | 作用 |
|------|------|
| `python run_all_daily.py` | 获取数据 → 构建特征 → 生成信号 |
| `python run_generate_report.py` | 生成回测报告HTML |

### 偶尔运行
| 命令 | 作用 |
|------|------|
| `python run_backtest_strategy_v3.py` | 重新跑回测 |
| `python run_fetch_news.py` | 获取新闻 |
| `python run_build_ai_briefs.py` | AI简报 |

### 核心文件清单
```
config_v31.yaml              ← 唯一配置
run_all_daily.py             ← 每日入口
run_generate_report.py       ← 报告生成（新版）
run_make_daily_rank.py       ← 信号生成（已修复）
run_backtest_strategy_v3.py  ← 回测
run_fetch_daily.py           ← 数据获取
run_build_features_daily.py  ← 特征构建
run_build_market_daily_all.py← 行情合并
quant/                       ← 核心库（不要动）
```

---

## 四、关于日期问题

**根本原因：** 之前用旧配置跑过（start_date可能是2017），生成的 `features_daily.parquet` 包含2017年数据。后来改了配置为2020-01-01，但没重新生成特征。

**新版报告脚本的处理：** 即使底层数据包含旧日期，`run_generate_report.py` 会自动读取 `config_v31.yaml` 中的 `start_date: 2020-01-01`，过滤掉旧数据并重新归一化权益曲线。

**彻底解决：** 执行第2步删除旧数据，重新跑流程即可。