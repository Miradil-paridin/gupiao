# 项目技术栈与目录结构说明

## 1. 文档目的
这份文档用于完整说明本项目的：
- 技术栈（语言、依赖、数据源、模型、输出）
- 目录结构（每个文件夹用途）
- 数据形成过程（从抓取到信号、回测、纸面交易、报告）
- 所有 Python 文件（`77` 个）逐个用途说明

说明：按你的要求，本文不展开逐个 `csv/parquet/json` 数据文件明细，但会详细说明它们是如何形成的。

---

## 2. 技术栈

### 2.1 语言与运行环境
- `Python 3.x`
- `PowerShell`（Windows 环境下常用运行入口）
- 虚拟环境：`.venv`（主环境）、`.venv-qlib`（Qlib 相关）

### 2.2 核心 Python 依赖（requirements.txt）
- `akshare`
- `pandas`
- `pyarrow`
- `pyyaml`
- `tqdm`
- `baostock`
- `requests`
- `python-dotenv`
- `openai`

### 2.3 代码中实际使用到的重要库
- 数值计算与统计：`numpy`
- 回测/时间序列切分：`scikit-learn`（`TimeSeriesSplit`）
- 可视化：`matplotlib`（可选）
- 量化框架对接：`qlib`（可选）
- 数据格式：`csv/parquet/json/jsonl/yaml`

### 2.4 外部数据源与接口
- 日线行情：`baostock`、`akshare`、`sina`
- 新闻源：`eastmoney`、`cls`、`sina`、`ths`
- 指数数据：沪深300（主要通过 `baostock`）
- 证据数据（可选链路）：公告、研报、股东数据（通过 `akshare` 相关接口）

### 2.5 AI/LLM 能力
- Provider：
  - `DeepSeek`
  - `MiMo`
- 调用层：
  - `llm_multi.py`（多模型统一入口）
  - `quant/llm_deepseek.py`（DeepSeek 单入口）

### 2.6 输出物形态
- 策略信号：`data/signals/*.csv`
- 回测结果：`data/backtests/*.csv/*.json/*.md`
- 纸面交易：`data/paper/*.json/*.csv`
- 新闻与简报：`data/news/*`、`data/briefs/*`
- 报告与看板：`data/reports/*.md`、`out/*.html`、`out/charts/*`

---

## 3. 目录结构说明（文件夹级）

## 3.1 项目根目录文件夹
| 文件夹 | 作用 |
|---|---|
| `.claude/` | Agent/辅助配置目录（非策略主逻辑）。 |
| `.idea/` | IDE（PyCharm）工程配置。 |
| `.venv/` | 主 Python 虚拟环境。 |
| `.venv-qlib/` | Qlib 相关虚拟环境。 |
| `__pycache__/` | Python 缓存目录。 |
| `_archive/` | 已归档的旧版本脚本（保留历史，不是主流程）。 |
| `_tmp_cfgs/` | 临时配置文件目录（实验/调参用）。 |
| `clean/` | 历史/过渡数据目录（与 `data/clean` 并存）。 |
| `data/` | 主数据目录（原始、清洗、特征、信号、回测、新闻、报告等）。 |
| `out/` | 前端展示输出（HTML 仪表盘、图表）。 |
| `quant/` | 核心业务包（数据、特征、信号、新闻、回测、报告组件）。 |
| `tools/` | 策略治理与评估工具脚本。 |

## 3.2 `data/` 子目录
| 文件夹 | 作用 |
|---|---|
| `data/raw/` | 原始抓取结果（按股票分文件）。 |
| `data/clean/` | 清洗后单票/聚合行情。 |
| `data/clean_qc/` | 质量修复后的行情聚合数据。 |
| `data/features/` | 特征工程结果（`features_daily.parquet`）。 |
| `data/signals/` | 每日排名与动作信号（`latest_daily_rank.csv`）。 |
| `data/backtests/` | 回测净值、交易、统计、三层评估、治理输出。 |
| `data/paper/` | Paper trading 状态、交易流水、权益曲线。 |
| `data/news/` | 按日期存储新闻聚合数据与 `manifest/qc_report`。 |
| `data/briefs/` | AI brief 产物（按日期 `ALL.json` 等）。 |
| `data/reports/` | Markdown 报告（日报、AI报告）。 |
| `data/index/` | 指数数据（如沪深300）。 |
| `data/evidence/` | 可选“证据抓取”结果（公告/研报/股东）。 |
| `data/logs/` | 全流程日志。 |

## 3.3 `out/` 子目录
| 文件夹 | 作用 |
|---|---|
| `out/charts/` | 图表图片（个股、行业热力图、权益曲线等）。 |

## 3.4 `quant/` 子目录
| 文件夹 | 作用 |
|---|---|
| `quant/providers/` | 行情数据源适配层。 |
| `quant/news_providers/` | 新闻数据源适配层。 |
| `quant/__pycache__/` | Python 缓存。 |

---

## 4. 数据形成过程（重点）

## 4.1 总入口：`run_all_daily.py`
`run_all_daily.py` 把链路串成三段：
1. 数据抓取与特征工程  
2. 新闻与资讯（含可选历史回补）  
3. 回测/治理/Paper/报告与仪表盘

## 4.2 详细链路
1. 股票池准备（周频/手动触发）
- `run_update_watchlist.py`：从全市场筛选出动态股票池，输出 `watchlist_cache.yaml` 与 `data/watchlist_report.csv`。
- `sync_watchlist.py`：把 `watchlist_cache.yaml` 同步到 `config.yaml/config_v31.yaml`。

2. 日线行情抓取与清洗
- `run_fetch_daily.py` 调 `quant.fetch_daily.fetch_daily_for_watchlist()`。
- 对每个股票：
  - 抓取原始行情（多源 fallback：`baostock -> akshare -> sina`）
  - 标准化字段（`quant.normalize`）
  - 写入 `data/raw/market_daily/*.csv`（可读原始）和 `data/clean/market_daily/*.parquet`（分析主数据）
- 如开启 `--qc`：
  - `run_qc_repair_market_daily.py` 调 `quant.qc_repair` 对异常数据二次拉取修复。

3. 全市场聚合
- `run_build_market_daily_all.py` 调 `quant.aggregate.build_market_daily_all()`
- 生成聚合行情：`data/clean/market_daily_all.parquet`

4. 特征工程
- `run_build_features_daily.py` 调 `quant.features.build_features_daily()`
- 生成特征（动量、均线、波动、ATR、RSI、涨停回调、TDX等）
- 输出：`data/features/features_daily.parquet`
- 同时尝试更新市场状态指数（`quant.market_regime`，沪深300）

5. 每日信号生成
- `run_make_daily_rank.py`（复用 `run_backtest_strategy_v3.py` 的核心选股逻辑）
- 产出：
  - `data/signals/daily_rank_<date>.csv`
  - `data/signals/latest_daily_rank.csv`
- 包含 `action`（`INVEST_MORE/HOLD/REDUCE/WITHDRAW/LEAST`）和 `target_weight`

6. 新闻抓取与清洗
- `run_fetch_news.py` 调 `quant.news_aggregator.run_fetch_all_news()`
- 默认抓“信号前 N 只 + 市场新闻”，支持 `NEWS_SYMBOLS` 精准模式
- 输出目录：`data/news/<date>/`
- 输出结构：
  - 每票新闻 `*.json`
  - 市场新闻 `_market.json`
  - `manifest.json`
  - QC 后 `qc_report.json`（去重、去乱码、去过旧、去无时间）

7. AI Brief 组装
- `run_build_ai_briefs.py` 调 `quant.briefs.build_ai_briefs()`
- 读取信号与新闻，生成按票摘要和全量 bundle
- 输出：`data/briefs/<date>/ALL.json`（后续日报/AI报告主输入）

8. 快速回测（可选）
- `run_backtest_strategy_v3.py`（由 `run_all_daily.py` 触发 quick backtest）
- 核心输出：
  - `data/backtests/backtest_strategy_v3_equity.csv`
  - `data/backtests/backtest_strategy_v3_trades.csv`
  - `data/backtests/backtest_strategy_v3_stats.json`
  - 三层评估：`backtest_strategy_v3_three_layer_report.(json/md)`
  - 可选 walk-forward：`backtest_strategy_v3_walk_forward.csv`

9. 策略治理（可选）
- `tools/strategy_process_pipeline.py`
- 基于回测结果做：
  - walk-forward 稳定性
  - rolling 稳定性
  - 参数稳定区
  - 过拟合诊断（PBO/DSR）
  - `quality_gate` 与 `trade_gate`
- 输出：
  - `data/backtests/strategy_process_summary.json`
  - `data/backtests/strategy_process_report.md`
  - 相关 `csv` 明细

10. Paper trading 记账
- `run_paper_trading.py`
- 输入：`latest_daily_rank.csv` + 市场行情 +（可选）策略闸门 cap
- 执行：先卖后买、整手约束、滑点/费用、挂单滚动
- 输出：
  - `data/paper/state.json`
  - `data/paper/trades.csv`
  - `data/paper/equity.csv`

11. 报告与可视化
- `run_generate_daily_report.py`：从 `data/briefs/<date>/ALL.json` 生成日报 markdown
- `run_ai_report_v2.py`：生成 AI 研报 markdown（可选 LLM 增强）
- `run_generate_dashboard.py`：生成信号仪表盘 `out/daily_dashboard.html`
- `run_generate_ai_dashboard.py`：生成 AI 仪表盘 `out/ai_report_dashboard.html`
- `run_generate_trade_journal_dashboard.py`：生成交易日志网页
- `run_generate_report.py`：生成回测 HTML 报告 `out/backtest_report.html`

12. 新闻历史回补（按需）
- `run_backfill_news_history.py`：按日期区间补抓历史新闻，支持限流（`--max-days/--max-symbols`）

13. 证据抓取链路（可选）
- `run_fetch_evidence.py` + `quant.fetch_evidence`：
  - 个股新闻、公告、研报、股东信息
  - 输出 `data/evidence/<date>/...` 与 `manifest.json`

---

## 5. Python 文件职责（全量 77 个）

## 5.1 根目录脚本（30）
| 文件 | 作用 |
|---|---|
| `_tmp_run_backtest_head.py` | 临时/实验版回测主逻辑脚本，用于策略头部开发与验证。 |
| `cleanup_project.py` | 项目清理预览脚本，列出可删文件与目录（默认不执行删除）。 |
| `llm_multi.py` | 多 LLM 统一调用层，封装 DeepSeek/MiMo 的 client 与重试。 |
| `report_advanced_metrics.py` | 高级绩效与交易指标计算模块，供报告生成调用。 |
| `run_ai_report_v2.py` | 生成 AI 研报（结构化内容 + 图表 + 质量评估 + 可选 LLM 增强）。 |
| `run_all_daily.py` | 日常总调度入口，串联数据、信号、新闻、回测、治理、paper、报告。 |
| `run_backfill_news_history.py` | 新闻历史区间回补脚本，支持交易日过滤与限流参数。 |
| `run_backtest_strategy_v3.py` | 核心 V3 回测引擎，支持大量参数开关、执行现实约束、三层评估。 |
| `run_build_ai_briefs.py` | 基于最新信号构建 AI briefs，输出 `data/briefs/<date>/ALL.json`。 |
| `run_build_features_daily.py` | 从聚合行情构建特征，并更新市场状态指数数据。 |
| `run_build_market_daily_all.py` | 合并单票清洗行情到全市场聚合行情。 |
| `run_check_data_quality.py` | 数据质量检查（日期范围、缺失交易日、异常值、连续性等）。 |
| `run_dynamic_stock_filter.py` | 动态股票筛选脚本（TDX规则），属于独立筛池工具。 |
| `run_fetch_daily.py` | 行情抓取入口（读取配置、设置 provider、调用量化抓取模块）。 |
| `run_fetch_evidence.py` | 证据抓取入口（从行情库推断 symbols，再抓公告/研报/新闻等）。 |
| `run_fetch_news.py` | 新闻抓取入口，含结构保持写回与 QC 清洗流程。 |
| `run_generate_ai_dashboard.py` | 将 AI 研报 markdown 渲染为 HTML 仪表盘。 |
| `run_generate_daily_report.py` | 生成日度 Markdown 报告，并处理“信号日/新闻日”别名文件。 |
| `run_generate_dashboard.py` | 生成每日信号 HTML 仪表盘。 |
| `run_generate_report.py` | 生成回测 HTML 报告（整合净值、交易、基准、指标）。 |
| `run_generate_trade_journal_dashboard.py` | 生成交易日志可视化页面（含统计与明细面板）。 |
| `run_make_daily_rank.py` | 每日信号排名生成器（与 V3 回测逻辑对齐）。 |
| `run_paper_trading.py` | 纸面交易记账引擎（状态机、订单调度、费用滑点、权益输出）。 |
| `run_qc_repair_market_daily.py` | 行情数据 QC 修复入口（坏数据重抓与回写）。 |
| `run_qlib_backtest.py` | Qlib 适配回测脚本（将外部信号格式转为 Qlib 输入）。 |
| `run_update_watchlist.py` | 智能股票池更新器（全市场过滤 + 打分 + 输出缓存池）。 |
| `run_validate_data_sources.py` | 多数据源交叉验证脚本（价格、成交量、日期一致性）。 |
| `run_validate_market_daily.py` | 对本地行情库做结构化验证并输出红旗项。 |
| `sync_watchlist.py` | 把 `watchlist_cache.yaml` 同步到配置文件中的 `watchlist`。 |
| `test_baostock.py` | BaoStock 连通性与基础拉数测试脚本。 |

## 5.2 `quant/` 根模块（23）
| 文件 | 作用 |
|---|---|
| `quant/__init__.py` | 包导出入口，暴露 logger/provider 常用接口。 |
| `quant/aggregate.py` | 单票行情 parquet 聚合为 `market_daily_all.parquet`。 |
| `quant/backtest_v3.py` | 轻量回测模块（非主入口版本），含结果对象与输出函数。 |
| `quant/briefs.py` | 从信号与新闻构建个股 brief 和 `ALL.json` 总包。 |
| `quant/features.py` | 特征工程主模块（收益、均线、波动、涨停回调、TDX等）。 |
| `quant/fetch_daily.py` | 行情抓取核心逻辑（增量更新、多源 fallback、熔断与失败清单）。 |
| `quant/fetch_evidence.py` | 证据抓取核心逻辑（新闻、公告、研报、股东数据）。 |
| `quant/llm_deepseek.py` | DeepSeek 专用 LLM 调用封装。 |
| `quant/logger.py` | 全项目日志初始化与复用。 |
| `quant/market_regime.py` | 沪深300抓取与市场状态判定（BULL/BEAR/NEUTRAL）。 |
| `quant/news_aggregator.py` | 多新闻源聚合、去重、情绪与关键词提取、落盘。 |
| `quant/normalize.py` | 各数据源字段映射与标准化输出。 |
| `quant/providers_akshare.py` | AkShare 抓取旧兼容工具函数（历史遗留）。 |
| `quant/qc_repair.py` | 行情质量修复模块（识别无效行并重抓修复）。 |
| `quant/report_charts.py` | 报告图表生成函数集（价格、指标、热力图、权益曲线）。 |
| `quant/report_generator.py` | AI 研报结构模板与内容拼装器。 |
| `quant/report_quality.py` | AI 研报质量评估与注释增强。 |
| `quant/reporting.py` | 从 briefs bundle 生成 Markdown 日报。 |
| `quant/signals.py` | 较早版信号模块（V3.1 口径）与动作分配逻辑。 |
| `quant/store.py` | 数据落盘工具（目录创建、读写 csv/parquet）。 |
| `quant/symbols.py` | 股票代码标准化与纯代码提取。 |
| `quant/tdx_indicators.py` | 通达信指标 Python 化实现并回填到日线特征。 |
| `quant/validate.py` | 行情库验证（重复、无效、缺失交易日）。 |

## 5.3 `quant/providers/`（5）
| 文件 | 作用 |
|---|---|
| `quant/providers/__init__.py` | Provider 工厂与注册表。 |
| `quant/providers/base.py` | Provider 抽象基类、统一异常与重试装饰器。 |
| `quant/providers/akshare_provider.py` | AkShare 行情抓取实现（含限速与错误处理）。 |
| `quant/providers/baostock_provider.py` | BaoStock 行情抓取实现（全局登录复用、限速）。 |
| `quant/providers/sina_provider.py` | Sina 行情抓取实现（公共接口适配）。 |

## 5.4 `quant/news_providers/`（6）
| 文件 | 作用 |
|---|---|
| `quant/news_providers/__init__.py` | 新闻 Provider 工厂与注册表。 |
| `quant/news_providers/base.py` | 新闻数据结构（`NewsItem`）与抽象基类。 |
| `quant/news_providers/cls_provider.py` | 财联社新闻源适配。 |
| `quant/news_providers/eastmoney_provider.py` | 东方财富新闻源适配。 |
| `quant/news_providers/sina_provider.py` | 新浪新闻源适配。 |
| `quant/news_providers/ths_provider.py` | 同花顺新闻源适配。 |

## 5.5 `tools/`（6）
| 文件 | 作用 |
|---|---|
| `tools/build_signal_history.py` | 从历史特征回放构建历史信号序列。 |
| `tools/calc_sharpe.py` | 通用 Sharpe 计算与回测文件自动发现工具。 |
| `tools/eval_robust_suite.py` | 稳健性评估套件（回测 + 指标 + 场景比较）。 |
| `tools/report_metrics.py` | 对单个回测文件输出核心绩效指标。 |
| `tools/run_backtest_from_signal.py` | 基于信号文件直接回测（含止损止盈等控制）。 |
| `tools/strategy_process_pipeline.py` | 策略治理主管线（WF/滚动/稳定区/PBO/DSR/gate）。 |

## 5.6 `_archive/`（7）
| 文件 | 作用 |
|---|---|
| `_archive/run_ai_report.py` | 旧版 AI 报告脚本（多模型版）。 |
| `_archive/run_backtest_report.py` | 旧版“回测+网页报告”一体脚本。 |
| `_archive/run_backtest_strategy_v2.py` | 旧版 V2 回测策略。 |
| `_archive/run_deepseek_report.py` | 旧版 DeepSeek 专用报告脚本。 |
| `_archive/run_generate_chart_v2.py` | 旧版权益曲线图生成器。 |
| `_archive/run_generate_chart_v3.py` | 旧版对比图生成器（v2/v3）。 |
| `_archive/run_make_daily_rank_v31.py` | 旧版 V3.1 信号生成脚本。 |

---

## 6. 运行建议（结合当前结构）
1. 每日主流程：`python run_all_daily.py`
2. 每周股票池更新：`python run_update_watchlist.py` + `python sync_watchlist.py`
3. 回测核验：`python run_backtest_strategy_v3.py`
4. 治理核验：`python tools/strategy_process_pipeline.py`

如果后续你愿意，我可以再补一份“极简运维版文档”（仅保留你日常需要执行的 10~15 个脚本，其他全部归档说明），让长期维护更省心。

